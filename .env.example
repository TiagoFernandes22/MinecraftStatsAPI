# Server Configuration
PORT=3000
NODE_ENV=development

# Admin Key (IMPORTANT: Change this to a secure random value in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_KEY=your-secure-admin-key-change-me-in-production

# Optional: Log Level
# Options: error, warn, info, debug
LOG_LEVEL=info
